# -------------------------------------------------------
# BuddyJudge Backend — Environment Variables
# -------------------------------------------------------
# Copy this file to .env and fill in real values locally.
# NEVER commit .env to version control.
# -------------------------------------------------------

# Google Gemini (AI evaluation — added in a future phase)
GEMINI_API_KEY=

# Supabase (database — added in a future phase)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
# NOTE: SUPABASE_SERVICE_ROLE_KEY must NEVER be exposed to the frontend.
